# Dependency lockfile for the analysis pipeline (Option A in README.md).
#
# These are the exact, fully-pinned versions used to run
# `figures/data-processing-and-charts.ipynb`. This set was verified on
# Python 3.12: re-running the notebook top to bottom reproduces the committed
# `dataset/baseline.csv` and `dataset/retry.csv` byte-for-byte.
#
# Reproduce the environment with:
#     python3 -m venv venv
#     source venv/bin/activate          # on Windows: venv\Scripts\activate
#     pip install -r requirements.txt
#
# Notes:
#   - `jupyter` is the notebook runner used to open and execute the notebook;
#     the analysis itself only depends on pandas, numpy, and matplotlib.
#   - The remaining entries are the transitive dependencies of matplotlib/pandas,
#     pinned so the environment resolves identically.

# --- Direct dependencies ---
pandas==3.0.3
numpy==2.5.1
matplotlib==3.11.0
jupyter==1.1.1

# --- Transitive dependencies (matplotlib / pandas) ---
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
packaging==26.2
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
